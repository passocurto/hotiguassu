@if(Request.IsAuthenticated) {
    <text> <i> Bem Vinda @User.Identity.Name </i>
     @Html.ActionLink("LogOff", "LogOff", "Girls") </text>
}
else {
    @: @Html.ActionLink("LogON", "LogOn", "Girls") 
}
